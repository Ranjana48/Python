killometer = float(input('Enter number of killometers driven : '))
rate = float(input(' Enter rate per killometer : '))
total_cost = killometer * rate
print(f'''
      The number of killometers driven {killometer}
      the rate per killometer {rate}
      The total rental cost is {total_cost}
      ''')